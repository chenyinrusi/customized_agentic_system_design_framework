# 设计理念与架构框架

## 1. 设计理念

### 1.1 明确目标："让 AI 成为可信赖的系统"
核心思想不是打造一个更聪明的 AI，而是构建一个多 Agent 协作的系统，让它安全、透明、可靠地完成工作。

- **信任优先**：产品必须可预测、可审计，而不是只追求炫酷。
- **人为主控**：每一个危险操作都应经过治理、审批和人工确认。
- **透明可见**：每一个决策、工具调用和成本事件都应可观察。
- **容错性强**：当某个 LLM 服务不可用时，系统必须继续运行。
- **记忆连续**：系统应记住项目决策，而不是仅保留一次性对话。

### 1.2 这个设计的不同之处

- 许多 AI 系统都把重点放在一个单一的助手上。
- 本项目有意做成模块化：每个 Agent 有专门职责，系统负责协调它们。
- 目标不是“AI 助手”，而是“AI 驱动的工程平台”。
- 价值在于管理风险、成本和持续工程工作，而不是只是生成结果。
- 系统明确把 Agent 的“大脑”与编排、执行和工具调用分离：我们把 LLM Mode 看作大脑，而 Agent 负责任务流程和外部工具。

这种分离意味着你可以在项目的任何部分、Agent 执行的任何步骤中更换 LLM Mode，从而在保密性、成本和质量之间做更精细的权衡。

## 2. 架构框架

### 2.1 核心层次结构

架构分为几个明确的层次：

1. **编排层**
   - 控制任务生命周期。
   - 管理多个 Agent 及其依赖关系。
   - 将任务分配给合适角色。

2. **推理层**
   - 负责生成计划和任务分解。
   - 使用更强的模型处理策略和审查任务。
   - 支持基于反馈的计划修订。

3. **执行层**
   - 负责代码生成、测试、审查和知识记录。
   - 执行工具调用，例如 shell 命令、文件写入、Git 操作和浏览器操作。

4. **治理层**
   - 强制执行 RBAC 和审批策略。
   - 记录审计事件和风险元数据。
   - 控制高风险操作和预算敏感决策。

5. **可观测层**
   - 记录所有工具调用和 Agent 状态。
   - 跟踪成本、Token 使用和执行链。
   - 让系统对用户可见。

6. **记忆层**
   - 持久化项目上下文和决策。
   - 提供结构化的跨会话记忆。
   - 通过压缩和清理保持历史记录可用。

### 2.2 Agent 角色划分

系统围绕专门化角色设计，而不是一个通用助手。**截至 2026 年 7 月，参考实现共有 27 个 Agent 角色**（原始设计为 6 个）。

六个基础角色：

- **Planning Agent**：分解目标、选择模型、编排任务。
- **Coding Agent**：编写代码、生成实现。
- **Research Agent**：搜索资料、分析架构、推荐方案。
- **Testing Agent**：编写并执行测试。
- **Review Agent**：审查代码质量、安全性和最佳实践。
- **Memory Agent**：记录项目决策、约束和历史上下文。

其余 21 个角色覆盖专门领域：

- **协调类**：Coordinator、Orchestrator — 任务路由与依赖管理。
- **基础设施**：DevOps、Security、Architect — 基础设施、安全加固、系统设计。
- **质量类**：Documentation、Data Engineer、Browser — 文档、数据管道、Web 自动化。
- **运维类**：Maintenance、Observability、Governance — 维护、监控、策略执行。
- **领域专属**：Kanban、Knowledge、Debate、Skills、Venture、Configuration、Sandbox、Channel（邮件/飞书）。

每个角色承担独立责任，并可并行运行。Agent 通过 `AgentRole` 数据类定义，包含角色专属系统提示、工具权限、LLM 预设和通信主题。Role Builder 支持在运行时动态创建新角色。

一个关键原则是：LLM Mode 的粒度要比 Agent 更小。这意味着可以把复杂步骤交给更贵、更强的 Mode，把简单步骤交给更便宜的 Mode；也可以把数据和任务拆分给不同的 Mode，让单一 AI 公司不会看到整个项目、数据或想法。

完整角色列表和演进细节参见 [§7. 2026 年 6 月以来的演进](#7-2026-年-6-月以来的演进)。

### 2.3 多 Agent 编排模型

- 系统使用任务队列和调度器来并行执行多个 Agent。
- 依赖关系被解析，使得安全性允许时可以并行推进。
- Agent 之间通过轻量消息总线和共享记忆交换结论。
- 最终由汇总步骤聚合结果，保证一致性。
- **多 Agent 辩论 (DD-47)**：多个 Agent 独立分析同一问题，然后比较并收敛到最优解。
- **分发模式**：系统支持 Orchestrator 主导分发（一个协调器路由任务）和自主分发（Agent 从共享 Goal Queue 中自选任务）。

### 2.4 治理与安全原则

- **先阻断，再许可**：危险操作默认阻止，须明确审批。
- **角色感知权限**：不同 Agent 和用户角色具有不同能力边界（RBAC）。
- **注入与速率防护**：每次工具调用都经过危险模式扫描（路径穿越、Shell 注入），并按 Agent 角色限速。
- **宪法不变性规则 (DD-48)**：CONSTITUTION.md 定义 9 条不可变规则——包括 Agent 不能禁用注入扫描、审计日志或降低质量评估门槛。任何 Agent 提示词或自编辑操作都不能覆盖这些规则。
- **预算感知**：模型选择受成本预算和使用阈值影响。
- **审计优先**：每次操作都记录上下文、结果和审批状态。

### 2.5 可观测与诊断

- 系统设计为支持运行时监控 Agent 状态。
- 仪表盘展示进度、Token 消耗、成本和待审批项。
- 执行链将用户请求与工具调用和 Agent 决策关联。
- 这种透明度是产品功能，而不是事后补充。

### 2.6 记忆与持续性

- 项目记忆保存决策、约束、架构笔记和风险。
- 记忆是结构化的，而不是原始聊天记录，分为三种类型：**共享记忆**（跨 Agent 持久存储）、**情景记忆**（会话范围内的交互记录）和 **FIFO 记忆**（最近上下文的滚动窗口）。
- 系统自动将相关记忆注入未来 Agent 提示。
- 通过老化、去重和压缩保持记忆有用且高效。

## 3. 高层产品叙事

### 3.1 解决的问题

目前多数 AI 开发工具要么：
- 是单次互动的黑箱助手；
- 要么是松散的工具集，缺乏长期项目连续性。

本产品填补了这之间的空白，提供：

- 多个专用 Agent 的并行工作能力
- 严格的操作安全和审批控制
- 透明的成本与行为跟踪
- 跨会话的持久项目记忆

### 3.2 价值主张

- 通过并行 Agent 提高交付速度。
- 通过治理控制提升自动化安全性。
- 通过可观测降低使用风险。
- 通过记忆增强项目连续性。
- 通过模型路由和回退实现实际成本管理。

### 3.3 为什么这很重要

工程工作本质上是协作、迭代和有状态的。
一个丧失上下文或失控运行的系统，并不适合生产。
这个设计旨在把 AI 变成一个受控、可测量的合作伙伴，而不是一个实验性玩具。

## 4. 差异化设计选择

### 4.1 默认多 Agent

大多数系统把多 Agent 视为可选或实验性功能。
本设计把它作为解决非平凡工程问题的默认方式。

### 4.2 治理是第一级功能

不是后续才补上治理，而是假设每个操作都有可能需要审批。
这让产品更适合团队和企业使用。

### 4.3 记忆作为产品特性

系统不只是保留对话历史。
它保存的是跨天、跨任务的关键决策和约束。

### 4.4 失败即回退

当一个 API 挂掉时，系统不是停止，而是顺着备用模型继续。
这带来更稳定的用户体验。

### 4.5 可观测性建立信任

系统假设用户应该始终知道发生了什么以及为什么发生。
这正是从实验性工具迈向可信赖工程平台的核心区别。

### 4.6 宪法级自治理

系统强制执行 Agent 无法覆盖的不可变规则。CONSTITUTION.md 定义了自我修改的边界，阻止 Agent 禁用安全控制、审计日志或质量门禁。这是自主运行的基石安全机制：系统可以改变自己的代码，但不能改变自己的约束。

## 5. 如何在不公开源码的情况下表达

如果你想传达想法而不公开实现细节，可以这样描述：

- 说它是“一个多 Agent 工程平台”，而不是“一个 Python 项目”。
- 说它是“治理优先的 AI 系统”，而不是“一个库集合”。
- 说它是“项目记忆引擎”，而不是“一个数据库实现”。
- 说它是“成本感知模型路由器”，而不是“提供商回退表”。
- 说它是“宪法级自治理系统”，而不是“一个带约束的配置文件”。。

价值在于设计模式，本质不是代码本身。

---

## 6. 关键设计陈述

- “本产品把 AI 视为一组协作的专家，而不是一个单一的预言机。”
- “它从第一天起就为可观测、可审计、成本感知而设计。”
- “它记住项目决策，而不是让用户反复解释上下文。”
- “它通过备用模型和提供商回退来优雅地处理失败。”
- “它默认阻止危险操作，只有在用户批准后才执行。”

---


## 7. 2026 年 6 月以来的演进

参考实现（Customized Agentic System）自本设计框架首次发布以来，规模和能力都有了显著增长。设计理念和核心架构层次保持不变，但系统的规模和功能集已经大幅扩展。

### 数据增长

| 维度 | 2026 年 6 月 | 2026 年 7 月 |
|-----------|-----------|-----------|
| Agent 角色数 | 6 | **27** |
| 内置工具数 | 17 | **47（12 大类）** |
| 源码模块数 | ~15 | **36+** |
| 单元测试数 | ~3000 | **9823（0 失败/0 flaky）** |
| 测试覆盖率 | ~55% | **80%（~170 文件 100%）** |
| CI 门禁 | 基础 | **Ruff 0 · Mypy 0 · tsc 0** |
| E2E 测试 | 无 | **Playwright 148+ specs** |
| 设计文档 | 2 | **12 份，全部落地** |

### 6 月以来新增的模块

| 模块 | 说明 |
|--------|-------------|
| **src/harness** (DD-49) | 自治循环驱动 — 后台 Goal Queue 轮询、计划断点续传 |
| **src/eval** (DD-43) | EvalGate — LLM 作为评判者的质量评估门 |
| **src/debate** (DD-47) | 多 Agent 辩论 — 多个 Agent 对同一问题辩论，收敛到最优解 |
| **src/self_edit** (DD-46) | 自编辑循环 — Agent 审计和改进自己的代码与提示词 |
| **src/skills** (DD-23) | Skill 注册系统 — 可插拔的 Agent 能力 |
| **src/code_graph** | 多语言代码知识图谱，跨项目理解 |
| **src/autoresearch** | 自动研究管道，迭代优化 |
| **src/ingestion** | 文档导入管道（PDF / DOCX / PPTX / XLSX / 图片 / URL） |
| **src/venture** | Venture Architect — 商业发现管道 |
| **src/testing** | E2E 前端浏览器测试 Agent 基础设施 |
| **src/notifications** | HumanNotifier — 操作者告警和升级 |
| **src/maintenance** | 计划维护任务 |
| **src/auth** | SessionContext — 认证上下文 |
| **src/config** | 运营配置管理 |
| **src/approval** | Human-in-the-loop 审批工作流 |
| **src/audit** | StateTransitionEvent/Log — 统一审计追踪 |
| **integrations/channels** | 邮件机器人 & 飞书机器人渠道 |
| **MCP 工具集成** (W6-4.1) | 运行时注入 MCP（Model Context Protocol）工具 |

### 哪些没变

六层架构（编排 → 推理 → 执行 → 治理 → 可观测 → 记忆）被证明是稳固的。每个新模块都能归入其中一层，没有任何一层被拆分或移除。治理优先原则、基于角色的 Agent 模型、以及"设计时就考虑可观测"的方法都保持原状。

### 哪些变了

- **工具**：17 → 47。原始类别（文件、Git、Web、记忆、沙箱）扩展了浏览器自动化、邮件、日历、看板、知识搜索、文档导入、MCP 工具和 Agent 自省诊断。
- **角色**：6 → 27。原始的 Planner/Coding/Research/Testing/Review/Memory 增加了 21 个专门角色，包括 Coordinator、DevOps、Security、Architect、Documentation、Data Engineer 等。
- **自治理**：新增 CONSTITUTION.md（DD-48），定义 9 条不可变规则 — Agent 不能禁用注入扫描、审计日志或降低 EvalGate 质量门槛。
- **容错**：4 级 API Key 回退链（env → registry → .env → dotenv），加上 CostRouter 自动选择 Provider。
- **质量基础设施**：三层测试金字塔（L1 烟雾 / L2 API 契约 / L3 语义验证）、E2E Playwright 套件、全 CI 自动化。


## 致谢

感谢 Hunter Baun 创作的 Deepseek TUI，着实打消了我在开发上面的节制，让我能不担心 cost 的情况下迅速开发做出这个产品。

感谢 Andrej Karpathy 的 LLM Wiki，我的记忆系统就是基于他的 idea 做出来的，最近我还在研究他的 autoresearch。

感谢 DeepSeek，也是因为这个价格亲民，所以我才能开发出来这个项目，我觉得我们这个时代的人都要感谢 DeepSeek 的老板，梁文锋，没有他，是他守住了普通人在这个时代的 AI 使用权，没有他，哪来的 AI 平权。

最后的最后，其实我一点也不想感谢 Claude，它又贵，还有个 5 小时使用的 cap，太烦人了，不过有些时候，用别的模型解决不了的 bug，还真能被它给解决了，我的建议是，Claude 做设计和攻坚，平常开发还是用 DeepSeek，毕竟 1 块钱买个 80 分的产品，或是 100 块钱买个 90 分的产品，对于众多价格敏感型消费者来说，还是 DeepSeek 会更好一些。

本项目的视频介绍和计划分解演示可在 YouTube 上观看：[Customized Agentic System - Plan Decompose & Thanks（播放列表）](https://www.youtube.com/playlist?list=PLblsECFOUwOnwiZwgud0mXUf_diDoqRpe)。
